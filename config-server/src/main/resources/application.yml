spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:canfeng/config-repo.git
server:
  port: 3001

security:
  user:
    name: config-admin
    password: config-admin

