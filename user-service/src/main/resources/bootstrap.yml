spring:
  application:
    # 和git里的文件名对应
    name: user-service
  cloud:
    config:
      # 指明配置服务中心的网址
      uri: http://localhost:3001/
      # dev 开发环境配置文件 |  test 测试环境  |  pro 正式环境
      profile: default
      # 远程仓库的分支
      label: master
      username: config-admin
      password: config-admin